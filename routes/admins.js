const router = require('express').Router()
const adminsController = require('../controllers/admins')

//router.post('/admins', (request, response) => adminsController.addAdmin(request, response))

module.exports = router